# TextEditor
A minimal texteditor in C

Only Compatible With unix-like systems
In Windows you need to install bash :https://learn.microsoft.com/en-us/windows/wsl/about
clone the repository, in your desired folder
open the path in terminal
commands:
make
./sanju <your filepath>

if make is not recognized:
sudo apt update
sudo apt install make
make --version
